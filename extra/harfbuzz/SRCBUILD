# Maintainer: Ivailo Monev <xakepa10@gmail.com>

# http://www.linuxfromscratch.org/blfs/view/svn/general/harfbuzz.html

version=2.6.4
description="OpenType text shaping engine."
depends=('glib' 'freetype2' 'icu')
optdepends=('graphite' 'cairo')
sources=("http://www.freedesktop.org/software/harfbuzz/release/harfbuzz-$version.tar.xz")

src_compile() {
    cd "$SOURCE_DIR/harfbuzz-$version"

    _options+="--without-graphite2 --without-cairo"
    [ "$OPTIONAL_GRAPHITE" = "yes" ] && _options+=" --with-graphite2"
    [ "$OPTIONAL_CAIRO" = "yes" ] && _options+=" --with-cairo"
    ./configure --prefix=/usr \
        --with-gobject \
        --disable-gtk-doc-html \
        $_options
    make
}

src_check() {
    cd "$SOURCE_DIR/harfbuzz-$version"
    make check
}

src_install() {
    cd "$SOURCE_DIR/harfbuzz-$version"
    make DESTDIR="$INSTALL_DIR" install
}
