# Maintainer: Ivailo Monev <xakepa10@gmail.com>

# http://www.linuxfromscratch.org/blfs/view/svn/basicnet/lynx.html

version=2.8.9
description="Text browser for the World Wide Web"
depends=('ncurses' 'openssl')
backup=('etc/lynx.cfg')
sources=("https://invisible-mirror.net/archives/lynx/tarballs/lynx${version}rel.1.tar.bz2")

src_compile() {
    cd "$SOURCE_DIR/lynx${version}rel.1"

    ./configure --prefix=/usr \
        --sysconfdir=/etc/lynx \
        --with-zlib \
        --with-bzlib \
        --with-ssl \
        --with-screen=ncursesw \
        --enable-locale-charset
    make
}

src_install() {
    cd "$SOURCE_DIR/lynx${version}rel.1"
    make DESTDIR="$INSTALL_DIR" install
}
