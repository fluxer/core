# Maintainer: Ivailo Monev <xakepa10@gmail.com>

# http://www.linuxfromscratch.org/blfs/view/svn/general/gperf.html

version=3.0.4
description="Perfect hash function generator"
sources=("http://ftp.gnu.org/pub/gnu/gperf/gperf-$version.tar.gz"
        "http://ftp.gnu.org/pub/gnu/gperf/gperf-$version.tar.gz.sig")
pgpkeys=('45E59068F91BB79F')

src_compile() {
    cd "$SOURCE_DIR/gperf-$version"

    ./configure --prefix=/usr
    make -j1
}

src_check() {
    cd "$SOURCE_DIR/gperf-$version"
    make -j1 check
}

src_install() {
     cd "$SOURCE_DIR/gperf-$version"
    make -j1 DESTDIR="$INSTALL_DIR" install
}